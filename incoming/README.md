# Parches entrantes

Sube aquí archivos `.patch`. El workflow `.github/workflows/apply-incoming-patch.yml` intentará aplicar automáticamente el parche sobre `main`, crear el commit correspondiente y eliminar el archivo procesado.

Antes de subir un parche, confirma que fue generado desde la versión actual del repositorio.
